version: '2'

services:
  mysql:
    image: daocloud.io/mysql
    container_name: mysql 
    network_mode: host
    restart: always
    #ports:
        #- 3307:3306
    environment:
        MYSQL_USER: jsongo 
        MYSQL_PASSWORD: AAH7eWGFvd63et 
        MYSQL_ROOT_PASSWORD: B84NRrkYPXa9zk 
        VERSION: 1.0.1
    volumes:
        - /data/mysql:/var/lib/mysql
        - ./my.cnf:/etc/mysql/my.cnf
        #- /var/run/docker-mysqld:/var/run/mysqld
        # - /data/nagios/etc:/opt/nagios/etc
